'use client';
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
export default function RegisterPage() { const router = useRouter(); const [form, setForm] = useState({name:'', email:'', password:'', developer:false}); const [error, setError] = useState(''); async function submit(e: FormEvent) { e.preventDefault(); const response = await fetch('/api/auth/register', { method:'POST', headers:{'Content-Type':'application/json'}, body:JSON.stringify(form) }); if (!response.ok) { setError((await response.json()).error || 'Falha no cadastro'); return; } router.push('/dashboard'); } return <main className="mx-auto max-w-md px-4 py-20"><h1 className="text-4xl font-black text-white">Criar conta</h1><form onSubmit={submit} className="mt-8 space-y-4 rounded-2xl border border-white/10 bg-slate-900 p-6"><input required placeholder="Nome" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} className="w-full rounded-lg bg-slate-950 p-3 text-white" /><input required type="email" placeholder="E-mail" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} className="w-full rounded-lg bg-slate-950 p-3 text-white" /><input required minLength={8} type="password" placeholder="Senha (mínimo 8 caracteres)" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} className="w-full rounded-lg bg-slate-950 p-3 text-white" /><label className="flex gap-2 text-sm text-slate-300"><input type="checkbox" checked={form.developer} onChange={e=>setForm({...form,developer:e.target.checked})} /> Quero publicar aplicativos</label>{error && <p className="text-sm text-red-400">{error}</p>}<button className="w-full rounded-lg bg-brand p-3 font-semibold text-white">Cadastrar</button></form></main>; }
