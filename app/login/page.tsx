'use client';
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
import Link from 'next/link';
export default function LoginPage() { const router = useRouter(); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState(''); async function submit(e: FormEvent) { e.preventDefault(); setError(''); const response = await fetch('/api/auth/login', { method: 'POST', headers: {'Content-Type':'application/json'}, body: JSON.stringify({ email, password }) }); if (!response.ok) { setError((await response.json()).error || 'Falha no login'); return; } router.push('/dashboard'); } return <main className="mx-auto max-w-md px-4 py-20"><h1 className="text-4xl font-black text-white">Entrar</h1><form onSubmit={submit} className="mt-8 space-y-4 rounded-2xl border border-white/10 bg-slate-900 p-6"><input required type="email" placeholder="E-mail" value={email} onChange={e=>setEmail(e.target.value)} className="w-full rounded-lg bg-slate-950 p-3 text-white" /><input required type="password" placeholder="Senha" value={password} onChange={e=>setPassword(e.target.value)} className="w-full rounded-lg bg-slate-950 p-3 text-white" />{error && <p className="text-sm text-red-400">{error}</p>}<button className="w-full rounded-lg bg-brand p-3 font-semibold text-white">Entrar</button><p className="text-sm text-slate-400">Não tem conta? <Link href="/register" className="text-brand">Cadastre-se</Link></p></form></main>; }
